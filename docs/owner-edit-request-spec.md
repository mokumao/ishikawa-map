# 店舗情報 修正・削除依頼システム 仕様書（第5版）

> 店舗主からの「情報を修正してほしい／掲載をやめてほしい」という依頼を受け付け、
> 安全かつ正確にサイトへ反映するための仕組みの設計書。
> 店舗主向け依頼画面、Googleフォームへの受付、削除依頼の自動非表示化、管理者による再表示まで実装・動作確認済み。
> 修正依頼の項目別編集と、送信前の変更前・変更後確認まで実装済み。
> 初回本人確認、メール確認、自動反映、管理者向け記録・復元は未実装。

最終更新：2026-09-10

---

## 0. 基本方針

- 石川マップは**正確性を最優先**する。
- **修正依頼**：初回は管理者が店舗とメールアドレスの関係を確認する。確認済み店舗主は、
  メールで変更内容を最終確認した後、日常的な修正を速やかに自動反映できる仕組みを目標とする。
  導入初期は安全な項目から始め、運用が安定した後に自動化範囲を広げる。
- **削除依頼**：**スピードを優先**する。非表示（hidden）は安全かつ即復元可能なので、速やかに非表示化し、なりすまし等が疑われる場合のみ事後に確認する（店舗主の権利尊重）。
  - ただし**公共機関・チェーン店は高速自動削除の対象外**（管理者確認に回す）。
  - **`refused`（永久に掲載しない）は自動化せず、管理者のワンタップ確認を挟む**（`hidden`は自動）。
- 個人情報（連絡先・削除理由など）は**公開リポジトリに一切保存しない**。
- 運用しながら気づいた問題・改善点は都度相談して仕様を更新する。

---

## 1. 目的

- 店舗主が気軽に修正・削除を依頼できる窓口を作る
- 修正は正確性を担保して反映、削除は迅速に対応する
- 「直すと得がある」と思ってもらえる仕組みを組み込む（12章）

---

## 2. 全体アーキテクチャ

```
店舗主 → owner-request.html（店舗idを自動取得）→ 非公開Googleフォーム
                                                   ↓ フォーム送信トリガー
                                        Google Apps Script
                              ┌──────────────┴──────────────┐
                         修正・その他                    削除依頼
                         管理者へ通知              GitHubへ自動処理を依頼
                         （手動確認）                       ↓
                                                   GitHub Actions
                                                          ↓
                                             hidden化・履歴追記・push

管理者ページ → 再表示ボタン → Apps Script確認画面 → GitHub Actions
                                                        ↓
                                   published（テスト店舗はtest）へ復元
```

現行実装では、当初案にあった`gh-data`ブランチへの依頼メタ情報の定期同期は使用していない。
依頼本文と連絡先は非公開Googleフォームに保存し、削除・再表示に必要な個人情報を含まない値だけを
Apps ScriptからGitHubの`repository_dispatch`へ送る。

**個人情報の保管ルール（最重要）**：
- 店舗主のメールアドレス・電話番号・削除理由などの個人情報は、**Googleスプレッドシート（あなたしか見られない非公開の場所）にのみ保管**する。
- 現行実装では依頼一覧を公開リポジトリへ同期しない。GitHubへ送るのは、自動処理に必要な店舗番号、
  処理種別、匿名受付番号などの**個人情報を含まない値だけ**とする。
- 管理者が連絡先を確認したいときは、スプレッドシートを直接開く。

---

## 3. 店舗主側のフロー

### 入口
- `detail.html` の「店舗主様へ」ボタン（設置済み）を依頼フォームへのリンクにする。
- **ボタン押下時に、その店舗の `id` をURLパラメータ経由でフォームの隠し項目へ自動セット**する（店舗名の手入力による取り違えを防ぐ）。
- 実装済みの`owner-request.html`で削除・修正・その他を選択し、内容を既存の非公開GoogleフォームへPOSTする。連絡先や理由をURLへ含めず、公開リポジトリや端末へ保存しない。

### まず3択を提示
大きなボタンで「削除依頼」「修正依頼」「その他」を選んでもらう。

### A. 削除依頼
- 入力項目は最小限：理由（選択式＋自由記述、任意）／連絡先（任意）
- **削除の種類を選んでもらう**（4章）：
  - 「一時的に消したい」→ `hidden`
  - 「二度と載せないでほしい」→ `refused`
- 送信後「速やかに対応します」と表示

### B. 修正依頼
- 現在の内容（住所・営業時間・定休日・備考）を**あらかじめ入力済み**で表示し、直したい部分だけ書き換えてもらう。
- 連絡を受けることに同意した場合だけ、メールアドレスを入力してもらう（ただし2章のとおり公開リポジトリには保存しない）。
- 送信後「内容を確認のうえ反映します」と表示

---

## 4. 削除の状態の使い分け

削除依頼を反映するとき、既存の `status` を使い分ける：
- `hidden`（非表示）：一時的に消したい場合。データは残し、いつでも復元可能。**高速自動化の対象**（民間個人店のみ／5-B章）。
- `refused`（掲載拒否）：「二度と載せないでほしい」と明確な意思がある場合。再登録しない。**自動化せず、管理者のワンタップ確認を挟む**（強い状態で誤爆・悪用の代償が大きいため）。

いずれも `statusHistory` に理由・日時を記録する（管理者専用の非公開記録）。

---

## 5. 運営側のフロー

### 5-A. 修正依頼（初回確認＋段階的な自動化）

#### 5-A-1. 初回登録（管理者承認あり）

1. 店舗主向け画面に、店名、住所、営業時間、定休日、備考などの現在値を項目別に表示する。
   営業時間は開始時間・終了時間を10分刻みの時刻選択で入力し、変更時は開始・終了を必ず一組で指定する。
   曜日別営業や昼夜営業などは補足欄へ入力できるようにする。
2. 店舗主は変更したい欄だけを編集し、氏名とメールアドレスを入力して申請する。
3. 入力されたメールアドレスへ確認リンクを送り、申請者がそのメールを利用できることを確認する。
4. メール確認だけでは店舗主本人であることは確定しないため、初回だけ管理者が店舗との関係を確認する。
5. 問題がなければ、店舗`id`とメールアドレスの組み合わせを「確認済み店舗主」として非公開で登録する。
6. 初回の変更内容は管理者がBefore／Afterを確認して承認し、反映後に店舗主と管理者へ完了メールを送る。

#### 5-A-2. 確認済み店舗主による通常修正（将来の自動化目標）

1. 確認済み店舗主が、現在値の入った項目別フォームで変更したい欄だけを編集する。
2. 送信前の画面に変更前・変更後を表示し、「この内容で修正する」確認を求める。
3. 登録済みメールアドレスへ、有効期限付き・一度だけ使える最終確認リンクを送る。
4. 店舗主がリンク先で変更内容を再確認して承認したら、自動的にデータへ反映してpushする。
5. 反映結果を店舗主と管理者へメール通知する。
6. `revisionHistory`へ`type: "owner"`として公開可能な修正履歴を残し、管理者用の非公開記録には
   変更前、変更後、店舗`id`、処理日時、匿名受付番号、確認済みメールとの照合結果を残す。

#### 5-A-3. 段階導入と安全装置

- 導入初期は、営業時間、定休日、備考などの日常的な項目から自動反映を開始する。
- 店名、住所、移転、掲載状態、確認済みメールアドレスの変更は、当初は管理者確認に回す。
- 運用実績を確認し、安全に処理できると判断した項目から自動化範囲を広げ、最終的な完全自動化を目指す。
- 完全自動化後も、短時間の連続変更、極端に大きい変更、不正な形式、店舗情報と無関係な文章などは
  自動停止し、管理者確認へ回す。
- 管理者ページから直前の内容へ戻せる復元機能を用意する。
- 依頼の自由記述は命令として実行せず、常に未信頼の文字データとして扱う。

#### 5-A-4. 店舗写真の追加・修正（未実装）

##### 店舗主側の操作

- 修正依頼画面の店名、住所、営業時間、定休日、備考の下に「店舗写真」を設ける。
- 「石川マップには店舗写真を最大6枚まで掲載できます。写真の追加は任意です」と案内する。
- 1回の依頼で最大6枚まで選択できるようにする。
- 次の入力方法に対応する。
  - 端末に保存されている写真を選択する。
  - スマートフォンのカメラを起動して、その場で撮影する。
  - パソコンなどでクリップボードから写真を貼り付ける。
- 選択した写真は送信前にプレビューし、削除と並べ替えができるようにする。
- 各写真のプレビュー直下に、その写真専用の任意コメント欄を設ける。
- コメントは1枚ごとに100文字以内とし、「店舗入口」「店内のテーブル席」「ランチメニュー」など、
  写真の内容を補足する短い説明を入力できるようにする。
- 写真を並べ替えた場合も、コメントは対応する写真と一緒に移動する。
- 写真を削除した場合は、その写真のコメントも同時に削除する。
- 掲載例として、店舗の外観、店内、メニュー、商品、設備などを案内する。

##### 掲載しない写真

- 店舗主、従業員、客、通行人、子どもなど、立場や本人の同意にかかわらず、人物または顔が写った写真は掲載しない。
- 人物が小さく写り込んでいる場合、鏡やガラスへの映り込み、写真・ポスター・画面内の顔も判定対象とする。
- 車両番号、個人名、住所、電話番号、決済情報、本人確認書類など、公開に適さない個人情報が読み取れる写真は掲載しない。
- 店舗と無関係な写真、違法・危険・差別的・性的・暴力的な内容、第三者の権利を侵害する可能性がある写真は掲載しない。

##### 権利と同意の確認

- 送信前に「自分で撮影した、または石川マップへの掲載許可を持つ写真である」ことへの確認を必須にする。
- インターネットやSNSから保存した第三者の写真は、明確な掲載許可を確認できない限り受け付けない。
- 店舗主による確認があっても、石川マップ側の安全基準に合わない写真は掲載しない。

##### 自動検査と差し戻し

1. ブラウザ側で枚数、ファイル形式、容量、画像として読み込めるかを確認する。
2. アップロード時に画像を再生成し、位置情報、撮影日時、端末情報などのEXIFメタデータを除去する。
3. 非公開の一時保存先へ置き、人物・顔・個人情報・不適切内容の自動検査を行う。
4. 人物または顔を検出した写真は公開せず、「人物が写っているため掲載できません。別の写真を選んでください」と案内する。
5. 自動判定の確信度が低い写真は自動公開せず、管理者確認または店舗主への再提出に回す。
6. 検査を通過した写真だけを公開用画像へ変換し、店舗データへ登録する。

自動画像判定には誤検出・見逃しがあり得るため、「人物なし」と断定できない画像を安全側へ倒す。
人物検出を回避するためのぼかし加工は自動では行わず、人物の写っていない写真への差し替えを基本とする。

##### AIによる写真説明

- 検査を通過した写真には、AIが短い説明文の下書きを作成できる。
- 店舗主がコメントを入力した場合はその内容を優先し、AIが勝手に別の説明へ置き換えない。
- コメントが空欄の場合に限り、AIによる説明候補を提示できる。店舗主は採用、修正、未入力を選べる。
- 説明は「店舗外観」「店内の客席」「メニュー表」など、画像から確認できる客観的内容だけに限定する。
- 人気、品質、味、安全性、価格評価など、写真だけでは確認できない表現を生成しない。
- 店舗主が説明文を確認・修正して承認した後に送信する。
- AIが生成した説明を指示として扱わず、公開前に文字数・禁止表現・個人情報を検査する。
- 公開時はコメントを写真の説明文として表示し、画像の代替テキストにも利用する。ただし、代替テキストとして
  不十分な場合は、画像から確認できる客観的な補足をシステム側で追加する。

##### 保存・公開・削除

- 石川マップは静的サイトのため、公開画面からGitHubへ直接画像を書き込まない。
- 原本はアクセス制限された非公開ストレージへ一時保存し、検査済み画像だけを公開用ストレージまたは
  公開リポジトリへ移す。具体的な保存先は実装前に容量、費用、権限、バックアップ、削除方法を比較して決定する。
- 公開画像は表示に適した寸法と形式へ変換し、ランダム化したファイル名を使用する。
- 却下・期限切れ・取り下げとなった一時画像は、定めた保管期限後に自動削除する。
- 店舗主が写真の削除・差し替えを依頼できるようにし、変更内容を履歴へ残す。
- 公開または差し戻しの結果を店舗主と管理者へメールで通知する。

##### 段階的な実装

1. 最大6枚の選択、カメラ撮影、貼り付け、プレビュー、削除、並べ替えを実装する。
2. 非公開アップロード、権利確認、管理者による写真確認と公開を実装する。
3. EXIF除去、画像変換、人物・顔・不適切内容の自動検査を実装する。
4. AI説明文、店舗主の最終確認、自動公開、メール通知、履歴、復元を実装する。
5. 誤判定と運用実績を確認し、曖昧な写真だけ管理者へ回す運用に移行する。

ファイル形式、1枚あたりの上限容量、変換後の最大寸法、一時保存期間、画像判定サービス、
保存先と費用は未決定とし、実装開始前に比較して決める。

**現在の実装状況**：店舗主向け画面に現在値を入れた項目別フォームを表示し、変更された欄だけを抽出して、
送信前に変更前・変更後を確認できるところまで実装済み。確認後はGoogleフォームへ送信し、管理者へ通知する。
初回メール確認、確認済み店舗主の登録、メールリンクによる承認、承認後の自動反映、修正の復元機能は未実装であり、
現状は管理者が依頼内容を確認してCodex等との対話で修正する。

### 5-B. 削除依頼（スピード優先）
削除依頼は対象店舗の種別によって扱いを分ける：

- **民間の個人経営店（飲食店・美容室など）**：
  1. 依頼を検知したら、**速やかに `hidden` 化してpush**（目標：気づいたら当日〜翌日中）。非表示は安全・即復元可能なため、事前の管理者承認を待たずに実行してよい。
  2. 管理者へ**事後通知**（店舗名・種別・理由・処理日時）。
  3. なりすまし等が疑われる場合のみ事後に確認・対応（誤りなら `published` に戻すだけで即復元）。
- **公共機関・チェーン店（銀行・学校・郵便局・コンビニ各店・ガソリンチェーン・公共観光施設など）**：
  - 匿名フォームからの削除依頼で即消すのは不自然なため、**自動削除の対象外**。管理者確認に回す。
- **`refused`（永久掲載拒否）を選択した依頼**：
  - 店舗種別を問わず**自動化せず、管理者のワンタップ確認**を挟む。

> 対象の判定は、カテゴリ（金融／教育／観光／コンビニ／ガソリン等）や、公式・公的な `sourceUrl` を持つかどうかで機械的に振り分ける。判定に迷うものは安全側（管理者確認）に倒す。

**現在の実装状況**：削除依頼を受信して対象店舗を`hidden`化し、`statusHistory`を追記してpushする処理は実装済み。
ただし、民間個人店と公共機関・チェーン店を自動判別して処理を分ける機能は未実装。
「今後掲載しない」が選択された場合も、現時点では直ちに`refused`へ変更せず、`hidden`化したうえで
「今後の掲載拒否は管理者確認待ち」と履歴に記録する。

### 5-C. 管理者による再表示（実装済み）

1. 管理者ページの店舗履歴から赤い「再表示」を押す。
2. 管理者用GoogleアカウントだけがアクセスできるApps Script確認画面を開く。
3. 緑の「地図に再表示する」を押す。
4. Apps Scriptが使い捨て確認情報を検証し、GitHubへ再表示処理を依頼する。
5. GitHub Actionsが対象店舗を`published`へ戻す。公開テスト店舗（`id: 0`）は`test`へ戻す。
6. `statusHistory`へ再表示履歴を追記し、関連HTMLのキャッシュ番号とサイトマップを更新してpushする。

確認情報の有効期限は10分で、一度使用すると再利用できない。期限切れや失敗時は管理者ページから
「再表示」をやり直す。Apps Scriptは「自分のみ」公開のため、管理者用Googleアカウントでログインする。
同じブラウザで複数のGoogleアカウントを併用すると認証に失敗する場合があるため、管理専用の
ブラウザープロファイルを推奨する。

### 通知の方法
- Google Apps Scriptから管理者宛てにメール通知する機能は実装済み。
- 修正・その他の依頼は受付内容を通知し、削除依頼はGitHubへの自動処理要求を送った結果も通知する。
- 再表示の実行結果も管理者へ通知する。
- 管理者ページ上の「未処理／処理済みの修正依頼」一覧は未実装。
- 通知メールに個人情報を載せる場合は、公開されない管理者宛メールに限る。

---

## 6. 実装の実体（「AI」が何で・いつ動くか）

石川マップには常時稼働するサーバーがないため、「AI」の実体を明確にする。

- **削除依頼＝AI判断不要・自動処理を実装済み**：
  「指定 `id` を `hidden` にする」処理をGoogle Apps ScriptとGitHub Actionsで自動化済み（LLM不要）。
  公共/チェーンの振り分けは未実装で、`refused`は自動確定せず`hidden`に留める。
- **修正依頼＝受付・通知まで実装済み、本人確認と自動反映は未実装**：
  項目別フォームで構造化した変更は、確認済み店舗主によるメール承認後、AIを使わず決定的な処理として自動反映できる。
  AIは自由記述の整理、変更案の補助、異常候補の提示などに限定し、AIの判断だけで公開情報を書き換えない。
  現状は管理者が依頼内容を読み、AIとの対話を必要に応じて使いながら個別に修正する。

### 2026-09-10時点の削除・再表示実装

- `integrations/google-apps-script/owner-request-handler.gs`：フォーム回答を検証し、管理者へ通知してGitHubへ非公開情報を除いた処理要求を送る。
- `.github/workflows/process-owner-request.yml`：対象店舗を`hidden`へ変更し、サイトマップとキャッシュ番号を更新してmainへpushする。
- `scripts/process_owner_request.js`：店舗IDを厳密に検証し、`statusHistory`へ一般化した記録と匿名受付番号を追加する。
- 削除理由本文とメールアドレスはGoogleフォーム・管理者メールだけに残し、GitHubへは送らない。管理者ページの履歴には「削除理由あり／記載なし」のみ表示する。
- 「今後掲載しない」も最初は`hidden`とし、`refused`は管理者確認後に変更する。
- 管理者ページでは、`hidden`の店舗の履歴内にだけ「再表示」を表示する。管理者本人だけが
  利用できるApps Script確認画面で再確認した後、GitHubへ`restore`を自動送信する。
- 公開詳細ページの修正履歴には、`statusHistory`から非表示日と再表示日だけを表示する。
  削除理由、受付番号、メールアドレスは公開しない。

---

## 7. 個人情報・データ保管の技術的な仕組み

- Googleフォームの回答はGoogleスプレッドシート（非公開）に溜まる。**連絡先・削除理由などの個人情報はここにとどめる。**
- Google Apps Scriptは、削除・再表示処理に必要な店舗番号、匿名受付番号、処理種別など、
  **個人情報を含まない値だけ**をGitHubへ送信する。
- 当初の案にあった`gh-data`ブランチへの依頼メタ情報同期と、そのJSONを使う修正依頼一覧は未実装。
- 連絡先や依頼本文を確認するときは、非公開Googleフォームまたは管理者宛メールを使用する。

---

## 8. 要注意ルール（AIが承認前にフラグを立てる／主に修正依頼）

以下に該当する依頼は「⚠️要注意」フラグを付け、管理者が慎重に判断する（自動では通さない）：
1. 自由記述欄に、店舗情報と無関係な文章・命令口調・URL・コードらしき文字列が含まれる（インジェクション・スパム対策。AIはこの欄を指示として解釈しない）
2. 同一店舗に短期間（例：24時間以内）で複数の依頼
3. （削除）連絡先が一切なく、かつ短期間に複数回など不自然なパターン
4. 修正内容が大きい（店名変更・住所の大幅変更など、店舗の同一性に関わる変更）
5. 依頼どおり直すと既存の `sourceUrl`（公式サイト等の裏付け）と明らかに矛盾する

※削除依頼（民間個人店）はスピード優先で速やかに非表示化するが、上記2・3のような不自然なパターンが見えた場合は、非表示化後の事後確認を優先的に行う。

---

## 9. 本人確認の考え方

- 個人運営では完全な本人確認は困難という前提。
- 修正反映時は `revisionHistory` に「店舗主様からの申告により更新」と明記し出所を透明化。
- 削除は「まず速やかに非表示→疑わしい時のみ事後確認」で、店舗主の権利尊重とリスク管理を両立。
- 明らかな裏付け（公式サイト等）と矛盾する修正申告は、そのままでは反映しない。

---

## 10. 店主への確認連絡とメール記録

- 現行の手動運用では、管理者が確認を必要と判断した場合にだけ店主へ連絡する。
- 将来の自動化では、初回登録、修正内容の最終確認、反映完了の連絡をシステムから自動送信する。
- 確認メールには店舗名とBefore／Afterを明記し、有効期限付き・一度だけ使えるリンクを使用する。
- 店舗主とのメールアドレス、確認記録、やり取りは非公開のGoogle側に保存し、公開GitHubへ保存しない。

### 10-A. 店舗主に示す運営者情報と信頼性

店舗主が安心して修正依頼を送れるよう、店舗主向け画面と確認メールから次の情報を確認できるようにする。

- 運営者名
- 50代、うるま市石川在住であること
- 石川マップの運営目的
- 連絡用メールアドレス
- 修正・削除依頼の処理方法
- 個人情報を公開サイトや公開GitHubへ保存しない方針
- AIを利用する範囲と、管理者が確認・管理していること

運営目的は、地域の身近な店舗や出来事を分かりやすく届け、石川の活性化に役立つことと、
AIを活用した情報管理の学習・検証を両立する試験的な地域情報サイトであることを簡潔に伝える。
「継続できるか分からない」「運営費の援助を求めたい」といった内容は、初回の信頼案内には含めない。
顔写真、自宅住所、私用電話番号、生年月日など、信頼目的に対して過剰な個人情報は掲載しない。

---

## 11. スパム・いたずら対策

- Googleフォーム側で利用できる迷惑送信対策を確認し、必要に応じて有効化する（現行設定は未確認）。
- 1回の送信で複数店舗をまとめて操作できない構造にする。
- 8章の要注意フラグと組み合わせ、管理者が捌ける範囲に抑える。

---

## 12. インセンティブ設計（アイデア）

- 情報が正しく更新されると「⚠️要確認」マークが消える → 達成感
- フォーム案内文を「お店の魅力を正確に伝えられます」など前向きに
- 削除が速いこと自体が店主の安心感につながる

---

## 13. 実装状況

| 機能 | 状態 | 補足 |
|---|---|---|
| 店舗詳細の「店舗主様へ」導線 | 実装済み | 店舗`id`を依頼画面へ引き継ぐ |
| 店舗主向け依頼画面 | 実装済み | 削除・修正・その他を選択し、現在の店舗情報を表示 |
| 非公開Googleフォームへの保存 | 実装済み | 連絡先・理由を公開GitHubへ保存しない |
| Apps Scriptによる受付通知 | 実装済み | 管理者メールへの通知とGitHub連携 |
| 削除依頼の自動`hidden`化 | 実装済み | GitHub Actionsが履歴追記・pushまで実行 |
| 管理者ページの状態履歴表示 | 実装済み | `statusHistory`を表示 |
| 管理者による再表示 | 実装済み | 確認画面を経て`published`または`test`へ復元 |
| 削除対象の店舗種別自動判定 | 未実装 | 公共・チェーン等を管理者確認へ分ける処理が必要 |
| `refused`の管理者承認 | 一部実装 | 自動では`hidden`に留め、履歴へ確認待ちと記録 |
| 修正依頼の受付・通知 | 実装済み | 変更項目と変更前・変更後をGoogleフォームへ送信 |
| 現在値を使った項目別編集 | 実装済み | 店名、住所、営業時間、定休日、備考を変更した欄だけ送る。営業時間は開始・終了の時刻選択と補足欄を使用 |
| 送信前の変更前・変更後確認 | 実装済み | 変更された項目だけを確認画面に表示し、編集へ戻ることもできる |
| 初回メール確認・店舗主確認 | 未実装 | 初回だけ管理者が店舗との関係を確認する |
| 確認済み店舗主の非公開登録 | 未実装 | 店舗`id`とメールアドレスを安全に紐付ける |
| メールリンクによる最終確認 | 未実装 | Before／Afterを確認後、一度だけ使えるリンクで承認する |
| 確認済み店舗主の自動修正 | 未実装 | 日常項目から開始し、安定後に対象を拡大する |
| 管理者への変更結果・履歴通知 | 未実装 | 変更前後、日時、匿名受付番号等をメールで残す |
| 管理者による修正の復元 | 未実装 | 問題時に直前の内容へ戻せるようにする |
| 要注意フラグの自動判定 | 未実装 | 短期重複、矛盾、大幅変更等の判定が必要 |
| 店舗主向け運営者情報・信頼案内 | 未実装 | 運営者、目的、連絡先、個人情報・AI利用方針を表示 |
| 店舗写真・最大6枚の入力 | 未実装 | 選択、カメラ撮影、貼り付け、プレビュー、並べ替えに対応 |
| 写真ごとの任意コメント | 未実装 | 各写真の下に100文字以内の説明を入力し、写真と一緒に管理する |
| 写真の非公開一時保存 | 未実装 | 保存先、容量、費用、保管期限は実装前に決定 |
| 写真の権利確認 | 未実装 | 撮影者または掲載許可を持つことへの同意を必須化 |
| 人物・顔・個人情報の画像検査 | 未実装 | 検出時は拒否し、曖昧な写真は管理者確認へ回す |
| 写真のEXIF除去・公開用変換 | 未実装 | 位置情報等を除去し、表示用に再生成する |
| AIによる客観的な写真説明 | 未実装 | 店舗主確認後に使用し、推測や評価表現は禁止 |
| 写真の公開・差し替え・復元 | 未実装 | 結果をメール通知し、変更履歴を保存する |
| CAPTCHA等の追加対策 | 未確認・今後検討 | Googleフォーム側の実設定を運用時に確認する |
| インセンティブ表示 | 未実装（任意） | 将来案 |

---

## 14. 補足：普段の作業との一貫性

- **修正依頼**は、現行の手動運用ではpush前に管理者が確認する。将来は、初回に店舗とメールの関係を
  管理者が承認し、確認済み店舗主による日常修正はメールで本人が最終承認した後に自動反映する。
- **削除依頼（民間個人店・hidden）**のみ、非表示化が安全・即復元可能であることを根拠に、スピード優先で事前承認を省く例外とする（誤りは即復元できるためリスク許容範囲）。公共・チェーン・`refused` は例外に含めない。
- ただし現行実装には店舗種別の自動判定がないため、この方針を完全には実現できていない。判定機能を実装するまでは運用上の既知の差分として扱う。

---

## 15. 解決済み・保留の論点（記録）

- **【解決済み】多言語（note_en）問題**：第3版までは「備考修正時に英語版が置き去りになる」問題を挙げていたが、2026-08-16に多言語（英語・中国語）表示機能を完全削除し日本語専用にしたため消滅。対応不要。
- **【保留】プライバシー目的の削除と公開リポジトリの矛盾**：削除動機が「無断掲載が不快・知られたくない」の場合、`hidden` にしてもデータは公開リポジトリ（`restaurants-data.js`）に残るため、本質的には要望に応えきれない。徹底するなら該当エントリ自体の削除やリポジトリ非公開化が必要だが、「復元しやすさ」とのトレードオフがあるため**現時点では保留**。将来方針として頭に置く。
